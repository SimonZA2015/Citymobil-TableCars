import style from './Header.module.css';

const Header = (props) => {

    return (
        <div className={style.body}>
            <text>header</text>
        </div>
    )
};

export default Header;